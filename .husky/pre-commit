#!/bin/sh

# Pre-commit hook: lint-staged (ESLint sur fichiers stagés) + TypeCheck
echo "🔍 Running pre-commit checks..."

# Run lint-staged
pnpm exec lint-staged

# Run TypeScript check
echo "📝 Type checking..."
pnpm exec tsc --noEmit

echo "✅ Pre-commit checks passed!"
